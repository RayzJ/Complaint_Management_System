spring.datasource.url=jdbc:mysql://localhost:3306/cms?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Jhfr@05042004

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Explicitly set the Hibernate dialect to avoid "Unable to determine Dialect without JDBC metadata"
# If you use MySQL 8.x use MySQL8Dialect; adjust if you run a different DB.
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

server.port=8080

# JWT configuration (can be overridden by APP_JWT_SECRET environment variable)
# Generate a secure secret and set in production - do not use this default!
# JWT secret key (use a long random string in production)
app.jwt.secret=MySuperSecretJWTKey1234567890
app.jwt.expirationMs=3600000